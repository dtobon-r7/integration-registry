/**
 * Adapter contract surface (interface, return shapes, status enum, failure modes)
 * plus per-product integration fetchers implementing
 * {@link com.rapid7.integrationregistry.adapter.IntegrationAdapter}.
 */
package com.rapid7.integrationregistry.adapter;
