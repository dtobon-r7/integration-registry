# Briefing package — WP-04 Full-context read-path integration suite

Mode: **Fresh**. Branch/worktree: `worktree-track-09-wp-04` (this checkout).
Base for diff: `main` (HEAD before session = 25d6c8c).

## Gate 1 — Open questions
- Plan-specific: none (work plan declares none).
- T10 (inbound identity filter chain) is **NOT a blocker** for this suite. WP-04
  fakes adapters and explicitly excludes the 401/filter-chain path (non-goal;
  Plan 03 + T10 own it). The dashboard's "needs T10" note refers to running the
  *full* read path against *real* components — out of scope for this adapter-faking
  suite. The genuine 401-before-handler assertion stays deferred to T10.

## Gate 2 — Modules / files
- New integration test suite under
  `src/test/java/com/rapid7/integrationregistry/<...>/` (likely a new
  `integration` package or alongside controller tests — brainstorming/writing-plans
  picks exact path).
- Extends the existing `ValkeyTestContainer` base
  (`src/test/java/.../cache/ValkeyTestContainer.java`) — real Valkey, ADR-006.
- `@TestConfiguration` supplying **two hand-written `StubAdapter` `IntegrationAdapter`
  beans** (productName `InsightIDR` + `InsightConnect`); real `InsightConnectAdapter`
  (@Component) kept OUT of the faked set via a `TypeExcludeFilter`. (Mockito mocks were
  disqualified at execution time: `FanOutCoordinator` validates `productName()` in its
  constructor at boot, where a mock returns null and fails the context.) Reuse
  `@MockitoBean S3Client` + seeded bundle pattern from `IntegrationRegistryApplicationTests`.
- Purpose-built **test-only vendor-mapping bundle** (one vendor with 2+ vendor
  services across the two products) staged to the temp cache-dir for the
  vendor-scoped scenario; the other scenarios may use the mvp-seed bundle.
- Drive cache tiers by pre-seeding Valkey: write fresh (cache-hit), write
  stale-only (stale fallback), leave empty (miss/omission).
- Likely shared fixtures/helpers in `testsupport/`.

## Gate 3 — Test strategy
- Full `@SpringBootTest` context (coordinator + aggregator + service + controller
  wired for real) extending `ValkeyTestContainer` (real Valkey via Testcontainers,
  Docker required — ADR-006, aligns with TESTING.md).
- Adapters faked via Mockito stubs (per Gate 2).
- Drive HTTP via the Spring Boot 4 idiom: `RestTestClient` / MockMvc against the
  mounted routes (confirm idiom against existing tests).
- Six scenarios from scope §Exit criteria: cache-hit, cache-miss, partial+stale,
  partial+omission, all-adapter-failure (200), vendor-scoped multi-service.
- Assert composed OUTCOMES (envelope, metadata math, 404-vs-partial), NOT
  component internals (T07/T08 unit tests own those).

## Override recorded
Developer directed: integration tests use Testcontainers + a real Valkey instance.
This supersedes WP-04 line 21's stale "(no TestContainers)" parenthetical, which
predated ADR-006. Consistent with the repo's current binding conventions
(ADR-006 + TESTING.md).
